version: '3.8'

services:
  app:
    command: ["make", "dev"]
    ports:
      - "0.0.0.0:8080:8080"
    volumes:
      - app:/app
      - node_modules:/app/node_modules

volumes:
  app:
  node_modules: